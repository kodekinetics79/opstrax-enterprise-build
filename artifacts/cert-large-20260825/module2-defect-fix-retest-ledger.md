# Module 2 defect, fix, and retest ledger

Tenant: `CERT-LARGE-20260825`  
Certification surface: visible Google Chrome  
Module: Telematics and Live Operations

| ID | Severity | Browser/customer finding | Fix | Retest evidence | State |
|---|---|---|---|---|---|
| M2-001 | P0 | A staging frontend could display a verified runtime state while its API SHA or environment differed. | PR #93 requires exact 40-character frontend/API SHA equality and matching environments. It exposes `Mismatch` and keeps startup grace in `Starting`. | Chrome showed `Mismatch` for frontend `d0e01c9…` / API `b2be712…`, then `Starting`, then `Staging` only after exact API `d0e01c9…` became ready and startup grace cleared. | Retested on `d0e01c9…`; final-candidate retest pending |
| M2-002 | P1 | Dispatcher saw Diagnostics and Vehicle Intelligence controls despite lacking diagnostics permission. | PR #93 conditions Control Tower destination controls on their actual permissions. | Dispatcher Chrome showed GPS and Device Health, no Diagnostics or Vehicle Intelligence; direct `/obd-j1939` rendered Permission denied. | Retested on `d0e01c9…`; final-candidate retest pending |
| M2-003 | P1 | OBD/J1939 and details did not make immutable device identity primary. | PR #93 adds device serial as the leading OBD column and separates serial from model in the detail drawer. | GPS Chrome search for `CLHQ-DEV-0138` showed serial first in the row and drawer; OBD final-candidate retest pending. | Partial retest |
| M2-004 | P1 | Maintenance Manager was offered OBD/J1939 in navigation but the page rendered an access-restricted dead end. | PR #94 aligns the page with the semantic read policy already used by the route and API; direct-only checks remain on governed writes. | Final-candidate Maintenance Chrome retest pending. | Fixed; retest pending |
| M2-005 | P1 | Certification transport nonces were run-scoped, but customer event identities were not; repeating the harness returned unexpected 409 conflicts. | PR #94 adds a deterministic run tag to customer event, correlation, and J1939 source identities while preserving the deliberate replay/idempotency controls. | R4 was stopped after 1,196 unexpected 409 responses. R5 completed all 7,642 attempts without an unexpected identity conflict; both deliberate 409 controls passed. | Supporting ingest control closed; final rendered outcomes pending |
| M2-006 | P1 evidence integrity | R5's `future-fix` control expected 422 but received 200. The payload was generated near run start and executed about 17 minutes later, so its nominally future timestamp had become a valid historical timestamp inside the seven-day backlog window. | Merged PR #95 places the control 24 hours beyond its execution phase and fails closed before network submission if it ever ages into the API's five-minute acceptance window. The product API rule is unchanged. | R5 is preserved with the invalid oracle result. Corrected control R5C1 preflight matched `d0e01c9…` and returned the required HTTP 422; 38/38 harness tests and 19/19 targeted endpoint security tests passed. Exact merged candidate is `a0da774…`. | Closed |

No item is closed solely by automated tests. Each fixed customer-visible item requires final exact-SHA Chrome evidence.
