# OpsTrax Current State Audit

Audit date: 2026-06-27

| Area | Current Status | Evidence/File/API/Table | Problem | Enterprise Gap | Recommendation | Priority |
|---|---|---|---|---|---|---|
| Backend framework | Built and working | `backend-dotnet/Program.cs`, `backend-dotnet/Opstrax.Api.csproj` | Active backend is .NET 8 Minimal API with schema bootstrap on startup | No formal migration tool; schema is created by services at runtime | Keep P0 on startup-safe schema bootstrap, then move to migrations in P0-B | P0 |
| Frontend framework | Built and working | `frontend/package.json`, `frontend/src/App.tsx` | React 19 + Vite + Tailwind app builds cleanly | Some UX polish gaps remain, but shell is functional | Preserve shell, fix only verified blockers | P0 |
| ORM / migration system | Partially built | `backend-dotnet/Services/*.cs`, `backend/src/lib/db.ts` | PostgreSQL schema is created/altered by idempotent services, not a formal migration tool | Hard to diff/rollback schema changes | Define a migration plan and rollback strategy before expanding schema | P1 |
| PostgreSQL configuration | Built and working | `.env.example`, `backend-dotnet/Data/Database.cs`, `backend/src/lib/db.ts`, `docker-compose.yml` | Active runtime expects PostgreSQL via `PG_CONNECTION` / `ConnectionStrings__DefaultConnection` | Legacy docs still mention MySQL | Update docs and keep MySQL references quarantined as legacy | P0 |
| Existing database models | Built but fragmented | `backend-dotnet/Services/*SchemaService.cs` | Many tables exist across feature-specific schema services | Models are spread across bootstrap classes, not a canonical ERD | Produce master ERD and table inventory before new Stage 2 work | P0 |
| Existing migrations | Missing | No `migrations/` folder; runtime bootstrap services instead | No explicit rollback or version history | Enterprise DB governance gap | Create P0-B migration plan and then formal migrations | P1 |
| API routes/controllers/services | Built and working | `backend-dotnet/Controllers/EndpointMappings.cs`, `backend/src/modules/*` | Large API surface is already present | Route surface is broad but not yet normalized into bounded contexts | Keep P0 local; document module boundaries before deeper refactor | P1 |
| Frontend screens/modules | Built and working | `frontend/src/pages/*`, `frontend/src/modules/moduleConfig.ts` | Enterprise shell and major operational pages exist | Some pages are demo-heavy but the navigation is real | Preserve route map and fix only verified UX issues | P0 |
| Auth / RBAC / sessions | Built and working | `frontend/src/services/apiClient.ts`, `frontend/src/hooks/useAuth.tsx`, `backend-dotnet/Controllers/EndpointMappings.cs` | Bearer + CSRF + tenant headers are wired in the client | Legacy doc set still implies future JWT/RBAC work | Keep current session model, avoid bypasses, validate 401 handling | P0 |
| Tenant isolation strategy | Needs tenant isolation check | `tenant_id`, `company_id`, `X-Opstrax-Tenant-Id` headers, schema services | Tenant scoping exists, but consistency varies across modules | Need a consistent tenant-owned table rule and row-scoping audit | Audit every tenant table before Stage 2 expansion | P0 |
| AI / LLM implementation | Partially built | `frontend/src/services/aiApi.ts`, `backend-dotnet/Controllers/EndpointMappings.cs` | AI exists as copilots/recommendations | Not yet a governed autonomous event engine | Keep AI read-mostly and auditable; no direct business writes | P1 |
| IoT / telematics implementation | Partially built | `backend-dotnet/Services/TelemetrySchemaService.cs`, `frontend/src/services/telematicsService.ts` | Live telemetry, alerts, and device lifecycle exist | Command safety and event normalization need tighter governance | Formalize ingestion, replay, and command safety in docs | P1 |
| CRM / finance / revenue implementation | Partially built | `backend-dotnet/Services/RevenueSchemaService.cs`, `frontend/src/pages/Batch5FinancePage.tsx` | Revenue primitives and pricing/catalog tables exist | Lead-to-renewal commercial spine is incomplete | Document the CRM/revenue model before expanding tables | P1 |
| Platform admin implementation | Built and working | `backend-dotnet/Services/PlatformSchemaService.cs`, `frontend/src/pages/platform/*` | Platform RBAC, sessions, billing, and audit tables exist | Not yet separated cleanly in the architecture docs | Preserve separate platform vs tenant identity model | P0 |
| Test/build scripts | Built and working | `frontend/package.json`, `backend/package.json`, `backend-dotnet.Tests/Opstrax.Tests.csproj` | Build and test commands are present | No single canonical release script | Use local build/test gates as the Stage 2 proof loop | P0 |
| Local run commands | Built and working | `start-local.sh`, `docker-compose.yml` | Local compose path exists | README still documents old MySQL-era topology | Use the compose path and current env contract | P0 |
| Git branch / working tree | Built and working | `git branch --show-current`, `git status --short -- .` | Branch is `opstrax-product-main`; only unrelated untracked files exist | No release-blocking source dirt from this run | Leave unrelated untracked files alone | P0 |

