# Stage 10 Research and Reading Notes

| Area | Files/Docs Read | Key Finding | Risk | Stage 10 Decision |
|---|---|---|---|---|
| Stage 9 backend APIs | `backend-dotnet/Controllers/Stage9Endpoints.cs`, `backend-dotnet/Services/Stage9OperationalFoundationService.cs`, `backend-dotnet/Services/Stage9SchemaService.cs`, `docs/architecture/OPSTRAX_STAGE_9_API_CONTRACTS.md`, `docs/architecture/OPSTRAX_STAGE_9_SCHEMA_CHANGE_LOG.md` | The Stage 9 operational entities are persisted and exposed, but the surface is still fragmented across individual endpoints. | Medium | Add a summary/read-model endpoint for demo and mobile consumption. |
| Stage 9 mobile readiness | `docs/architecture/OPSTRAX_STAGE_9_MOBILE_READINESS_NOTES.md`, `docs/architecture/OPSTRAX_STAGE_9_MOBILE_READINESS_IMPLEMENTATION_NOTES.md` | Mobile readiness is expected at the backend level, but no mobile app exists yet. | Medium | Preserve mobile-safe contracts, retry semantics, and evidence metadata. |
| Stage 9 RBAC | `backend-dotnet/Controllers/EndpointMappings.cs`, `backend-dotnet/Foundation/FoundationServices.cs`, `frontend/src/hooks/usePermission.tsx`, `frontend/src/auth/rbacConfig.ts` | Authorization is centralized and fail-closed; frontend permission helpers already exist. | Medium | Reuse the canonical permission path and hide unauthorized actions in the UI. |
| Stage 9 POD / proof package | `backend-dotnet/Services/Stage9OperationalFoundationService.cs`, `docs/architecture/OPSTRAX_STAGE_9_POD_SITE_ACCESS_ARCHITECTURE.md` | Proof packages and proof artifacts are real but need a single narrative surface. | High | Build a proof-center page that reflects the actual persisted proof model. |
| Stage 9 smart assignment | `backend-dotnet/Services/Stage9OperationalFoundationService.cs`, `docs/architecture/OPSTRAX_STAGE_9_SMART_ASSIGN_ARCHITECTURE.md` | Smart assignment is recommendation-driven and approval-safe for risky actions. | Medium | Surface score, confidence, risk, and approval state in one panel. |
| Stage 9 site access / Gate Pass / NOC | `backend-dotnet/Services/Stage9OperationalFoundationService.cs`, `docs/architecture/OPSTRAX_STAGE_9_POD_SITE_ACCESS_ARCHITECTURE.md` | Access documents and site access requirements are stored but not yet demo-cohesive. | High | Provide a dedicated site-access panel with blockers and waiver visibility. |
| Stage 9 3P pickup | `backend-dotnet/Services/Stage9OperationalFoundationService.cs`, `docs/architecture/OPSTRAX_STAGE_9_POD_SITE_ACCESS_ARCHITECTURE.md` | Pickup authorization exists as a foundation, but user-facing presentation is still missing. | High | Add a third-party pickup panel with validity and verification cues. |
| Stage 9 warehouse handover | `backend-dotnet/Services/Stage9OperationalFoundationService.cs` | Warehouse handover persistence exists and can be surfaced safely. | Medium | Reuse the same workflow summary and show scheduled/completed status. |
| Stage 9 billing confidence | `backend-dotnet/Services/Stage9OperationalFoundationService.cs` | Billing confidence records can be read, but they are not yet part of a cohesive workflow view. | Medium | Show billing confidence as an indicator, not an automatic invoice action. |
| Frontend routing / navigation | `frontend/src/App.tsx`, `frontend/src/layouts/AppShell.tsx`, `frontend/src/modules/moduleConfig.ts`, `frontend/src/auth/sessionRouting.ts` | The app already has a single shell and permission-gated route map. | Low | Add a single proof-center route and a module entry. |
| Frontend auth / RBAC | `frontend/src/hooks/usePermission.tsx`, `frontend/src/auth/rbacConfig.ts`, `frontend/src/hooks/useAuth.tsx` | Permission helpers already support hiding and disabling actions based on session scope. | Low | Use read-only and action-gated UI states instead of frontend-only security. |
| Frontend API client | `frontend/src/services/apiClient.ts`, `frontend/src/services/fleetTmsApi.ts`, `frontend/src/services/dispatchApi.ts` | The shared Axios client already carries auth and tenant headers. | Low | Reuse the shared client and preserve tenant/company scoping. |
| Frontend UI patterns | `frontend/src/pages/JobsPage.tsx`, `frontend/src/pages/DispatchCommandPage.tsx`, `frontend/src/pages/Batch4SafetyPage.tsx`, `frontend/src/pages/Batch5FinancePage.tsx`, `frontend/src/components/ui.tsx` | Existing operational pages already use panel, summary, loading, error, and drawer patterns. | Low | Match those patterns so the new page feels native to the product. |
| Mobile API contract needs | Stage 9 docs plus the existing frontend and backend contracts | Future mobile clients need stable read models, tenant scoping, idempotency, and evidence metadata. | Medium | Create a consolidated summary API and document the field policy. |
| Demo workflow needs | Stage 9 docs, Stage 8 finance activation, current operational pages | Buyers need a single walkthrough from assignment to proof to billing confidence. | High | Build an Operational Proof Center with honest empty states. |
| Production safety | `backend-dotnet/Program.cs`, Stage 9 docs, frontend shell conventions | The repo already uses guarded startup and fail-closed route gating. | Low | Keep Stage 10 local-only and avoid production assumptions. |

