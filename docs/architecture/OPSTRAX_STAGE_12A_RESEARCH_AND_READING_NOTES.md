# Opstrax Stage 12A Research and Reading Notes

This document records the files and areas reviewed before Stage 12A implementation.

| Area | Files/Docs Read | What Exists | What Is Missing | Risk | Stage 12A Decision |
|---|---|---|---|---|---|
| Vehicles/assets | `backend-dotnet/Controllers/EndpointMappings.cs`, `backend-dotnet/Services/Stage9OperationalFoundationService.cs`, `frontend/src/pages/VehiclesModulePage.tsx`, `frontend/src/pages/LiveMapPage.tsx` | Vehicles, assets, and live map entities already exist with rich operational fields and tenant-scoped queries. | A durable telemetry-native live state projection and telemetry-linked alerting contract are still incomplete. | Medium | Reuse the current fleet domain and project telemetry into it safely. |
| Drivers/operators | `backend-dotnet/Controllers/EndpointMappings.cs`, `frontend/src/pages/DriversModulePage.tsx`, `frontend/src/pages/driver/*` | Driver identity, assignment, and mobile portal scaffolding already exist. | No telemetry-native driver context or mobile telemetry contract yet. | Medium | Keep driver-scoped telemetry fields role-safe and minimal. |
| Devices | `backend-dotnet/Services/TelemetrySchemaService.cs`, `backend-dotnet/Controllers/EndpointMappings.cs`, `frontend/src/pages/IotDevicesPage.tsx`, `frontend/src/services/telematicsService.ts` | Device registry, provisioning, and legacy HMAC ingest are already real. | A formal telemetry device API and richer device metadata model are missing. | Medium | Extend the registry rather than inventing a parallel provider stack. |
| Existing telemetry code | `backend-dotnet/Services/TelemetrySchemaService.cs`, `backend-dotnet/Services/TelemetryBackgroundService.cs`, `backend-dotnet/Services/TelemetrySimulatorBackgroundService.cs`, `backend-dotnet/Controllers/EndpointMappings.cs` | GPS ingest, latest positions, stale-device alerts, SSE stream, and dev simulator already exist. | The model is legacy-shaped and not yet formalized into the Stage 12A telemetry contract. | High | Formalize the existing telemetry path with additive schema and new read models. |
| GPS/location data | `backend-dotnet/Services/Batch1SchemaService.cs`, `backend-dotnet/Controllers/EndpointMappings.cs`, `frontend/src/hooks/useLiveTelemetry.ts` | `location_events` and `latest_vehicle_positions` already persist live movement and support SSE/live map. | No Stage 12A telemetry event abstraction or normalized live-state API yet. | Medium | Build on the persisted live-position backbone, not fake markers. |
| Sensor data | `backend-dotnet/Services/TelemetrySchemaService.cs`, `frontend/src/services/telematicsService.ts`, `frontend/src/pages/IotDevicesPage.tsx` | The UI already simulates sensor/diagnostic posture and device status. | No durable Stage 12A sensor event schema or alert bridge. | High | Add the sensor-event foundation only if it can stay deterministic and tenant-scoped. |
| Diagnostics data | `backend-dotnet/Services/TelemetrySchemaService.cs`, `backend-dotnet/Services/SafetyBackgroundService.cs` | Diagnostic codes and telemetry alerts are already represented in the legacy stack. | No normalized telemetry diagnostic ingestion API yet. | Medium | Keep diagnostics as a first-class event type in the additive contract. |
| Live Map | `frontend/src/pages/LiveMapPage.tsx`, `frontend/src/components/LiveMap.tsx`, `frontend/src/services/controlTowerApi.ts` | Live map exists and already merges SSE positions onto DB snapshots. | The frontend still depends on the legacy control-tower summary rather than a telemetry-native live-map contract. | Medium | Introduce a telemetry-backed live-map payload while keeping route compatibility. |
| Cockpit | `frontend/src/pages/CommandCenterPage.tsx`, `frontend/src/modules/moduleConfig.ts`, `frontend/src/layouts/AppShell.tsx` | Main operations cockpit is already branded and permission-gated. | No telemetry-focused cockpit widget yet. | Low | Add a small telemetry visibility block only where it improves the story. |
| Alert Center | `backend-dotnet/Controllers/EndpointMappings.cs`, `frontend/src/pages/AlertsCenterPage.tsx`, `backend-dotnet/Services/SafetyBackgroundService.cs` | Alerts already exist, including telemetry-derived alerts and triage views. | No telemetry-native alert API contract with Stage 12A names/statuses yet. | Medium | Reuse and extend the current alert path. |
| Dispatch/jobs/trips | `backend-dotnet/Controllers/EndpointMappings.cs`, `backend-dotnet/Services/TripBackgroundService.cs`, `frontend/src/pages/DispatchPage.tsx` | Dispatch and trip binding already consume live positions. | Telemetry is not yet a formal upstream contract for dispatch risk and live-state. | Medium | Keep telemetry read models compatible with dispatch and trip consumers. |
| Safety/incidents | `backend-dotnet/Services/SafetyBackgroundService.cs`, `frontend/src/pages/DriverScorecardsPage.tsx`, `frontend/src/pages/IncidentsPage.tsx` | Safety already converts telemetry alerts into safety events. | No Stage 12A telemetry recommendation bridge or live-state risk model yet. | Medium | Feed the governed AI foundation only for high-risk telemetry. |
| Maintenance/fleet health | `frontend/src/pages/VehicleHealthPage.tsx`, `frontend/src/pages/MaintenanceCommandPage.tsx`, `backend-dotnet/Services/MaintenanceBackgroundService.cs` | Maintenance and fleet health already exist. | No explicit telemetry-to-maintenance bridge in the formal contract. | Medium | Surface telemetry risk into maintenance read models instead of creating another silo. |
| POD/billing confidence | `docs/architecture/OPSTRAX_STAGE_10_*`, `docs/architecture/OPSTRAX_STAGE_11_*` | POD and billing-confidence stages already exist and are complete locally. | No Stage 12A telemetry bridge to those read models yet. | Low | Keep telemetry focused and let downstream stages consume it. |
| AI recommendation foundation | `backend-dotnet/Foundation/FoundationContracts.cs`, `backend-dotnet/Foundation/FoundationServices.cs`, `backend-dotnet/Foundation/FoundationPersistenceServices.cs`, `backend-dotnet/Foundation/FoundationDispatcherServices.cs` | Governed AI reasoning, recommendations, and action requests are DB-backed and approval-aware. | No telemetry-specific AI recommendation bridge yet. | Medium | Use the existing governed AI foundation only, and keep it recommendation-only. |
| Events/outbox | `backend-dotnet/Foundation/FoundationContracts.cs`, `backend-dotnet/Foundation/FoundationDispatcherServices.cs`, `backend-dotnet/Services/OutboxDispatcherBackgroundService.cs`, `database/migrations/2026_06_28_stage5d_p0b1a3_dispatcher.sql` | Domain events, outbox, inbox, and dispatcher runtime already exist. | Telemetry events are not yet emitted as first-class domain/outbox records. | Low | Emit telemetry events through the durable foundation path. |
| RBAC/permissions | `backend-dotnet/Controllers/EndpointMappings.cs`, `backend-dotnet/Program.cs`, `frontend/src/auth/rbacConfig.ts` | Centralized permission checks and aliasing already fail closed. | Telemetry-specific permission keys still need to be added end-to-end. | Medium | Add explicit telemetry permissions and keep permission enforcement centralized. |
| Tenant/company isolation | `backend-dotnet/Program.cs`, `backend-dotnet/Controllers/EndpointMappings.cs`, `frontend/src/services/apiClient.ts` | Tenant context is carried through bearer session + `X-Opstrax-Tenant-Id`. | New telemetry endpoints must not accept cross-tenant device or vehicle writes. | High | Require tenant-safe resolution and deny cross-tenant telemetry access. |
| Frontend data contracts | `frontend/src/services/apiClient.ts`, `frontend/src/services/controlTowerApi.ts`, `frontend/src/hooks/useLiveTelemetry.ts` | The app already has a strong API client and live telemetry hook pattern. | No telemetry-native frontend client exists yet. | Medium | Add a telemetry API wrapper instead of patching ad hoc requests everywhere. |
| Mobile-readiness | `docs/architecture/OPSTRAX_STAGE_10_MOBILE_API_CONTRACTS.md`, `docs/architecture/OPSTRAX_STAGE_10_MOBILE_READINESS_REVIEW.md` | Mobile-safe read models and idempotent patterns are already documented. | Telemetry/mobile role mapping still needs a formal contract. | Medium | Keep payloads minimal and role-scoped for future mobile use. |
| Sales/demo impact | `docs/architecture/OPSTRAX_STAGE_10_DELIVERY_ASSURANCE_REVIEW.md`, `docs/architecture/OPSTRAX_STAGE_11_COMPLETION_REPORT.md` | Stage 10 and 11 made the product demo-ready for operations and commercial control. | Without telemetry, the live-map story remains partially synthetic. | High | Make telemetry real enough to support the enterprise demo story without fake streams. |

