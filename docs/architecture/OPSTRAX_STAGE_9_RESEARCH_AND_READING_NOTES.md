# Stage 9 Research and Reading Notes

| Area | Files/Docs Read | Key Finding | Risk | Stage 9 Decision |
|---|---|---|---|---|
| Job/trip execution | `backend-dotnet/Services/BusinessSpineServices.cs`, `backend-dotnet/Services/TripSchemaService.cs`, `backend-dotnet/Controllers/EndpointMappings.cs` | Jobs, trips, and dispatch assignments already exist and are tenant-scoped, but proof/access foundations are still separate from the operational spine. | Medium | Extend the existing job/trip model; do not rebuild dispatch. |
| Dispatch/assignment | `backend-dotnet/Controllers/EndpointMappings.cs`, `backend-dotnet/Services/DispatchSchemaService.cs` | Dispatch already has assignment/accept/cancel workflows and permission gating. | Medium | Reuse dispatch permissions and emit new Stage 9 events from the proof layer. |
| Vehicle/driver availability | `backend-dotnet/Services/DriverSchemaService.cs`, `backend-dotnet/Services/DispatchSchemaService.cs`, `backend-dotnet/Controllers/EndpointMappings.cs` | Driver and vehicle availability already influence dispatch eligibility. | Medium | Smart assignment scoring can read these signals and degrade confidence when data is missing. |
| Proof of delivery/service | `backend-dotnet/Controllers/EndpointMappings.cs`, `backend-dotnet/Services/Batch2SchemaService.cs` | Existing POD is a placeholder centered on `proof_of_delivery`. It is not yet a canonical proof-package system. | High | Create a new proof-package foundation with evidence artifacts and validation. |
| Evidence/file handling | `backend-dotnet/Controllers/EndpointMappings.cs`, `backend-dotnet/Services/Batch3SchemaService.cs`, `backend-dotnet/Services/NotificationSchemaService.cs` | The repo has document/file-adjacent surfaces, but no dedicated Stage 9 proof artifact model. | High | Allow optional `file_id`, persist metadata, and document the file-service gap. |
| Gate pass/NOC | `docs/architecture/OPSTRAX_STAGE_9_MOBILE_READINESS_NOTES.md`, existing security/compliance services | No canonical gate-pass/NOC foundation exists yet. | High | Add access-document tables and event hooks with tenant isolation. |
| Warehouse pickup/handover | `backend-dotnet/Controllers/EndpointMappings.cs`, `backend-dotnet/Services/DispatchSchemaService.cs` | Handover behavior exists only indirectly through dispatch and proof placeholders. | High | Add a dedicated warehouse handover table and APIs. |
| Third-party pickup authorization | Same as above plus mobile notes | No first-class 3P pickup authorization model exists. | High | Add pickup authorization foundation with verification and expiry handling. |
| AI recommendation foundation | `backend-dotnet/Foundation/FoundationPersistenceServices.cs`, `backend-dotnet/Foundation/FoundationContracts.cs` | AI reasoning runs, recommendations, and action requests are DB-backed and approval-aware. | Medium | Use the governed AI foundation for operational risk liaisons only. |
| Approval workflow | `backend-dotnet/Foundation/FoundationPersistenceServices.cs`, `backend-dotnet/Foundation/FoundationContracts.cs` | Approval requests and decisions are persisted and already support high-risk finance actions. | Medium | Reuse the approval workflow for high-risk access/waive actions. |
| Revenue readiness dependency | `backend-dotnet/Services/RevenueReadinessService.cs`, `backend-dotnet/Controllers/RevenueReadinessEndpoints.cs` | Finance already depends on persisted jobs, charges, approvals, and AI recommendations. | Medium | Stage 9 should feed billing-confidence signals without auto-issuing invoices. |
| Authorization/RBAC | `backend-dotnet/Controllers/EndpointMappings.cs`, `backend-dotnet/Foundation/FoundationServices.cs` | Auth fails closed and permission aliasing is already centralized. | Medium | Add Stage 9 permission keys and keep the same decision engine. |
| Tenant/company isolation | `backend-dotnet/Controllers/EndpointMappings.cs`, `backend-dotnet/Services/*SchemaService.cs` | Most operational writes already filter by company/tenant. | Medium | Every new Stage 9 table and endpoint must carry company_id and fail closed. |
| Events/outbox | `backend-dotnet/Foundation/FoundationDispatcherServices.cs`, `backend-dotnet/Services/OutboxDispatcherBackgroundService.cs`, `backend-dotnet/Services/FoundationSchemaService.cs` | Domain events, outbox messages, inbox rows, and processing logs are persisted and dispatchable. | Low | Emit Stage 9 events through the existing durable event path. |
| Idempotency | `backend-dotnet/Foundation/FoundationPersistenceServices.cs`, `backend-dotnet/Services/FoundationSchemaService.cs` | DB-backed idempotency keys already exist and are suitable for replay-safe mobile submits. | Low | Reuse idempotency for create/submit/validate flows where retriable. |
| Audit/correlation | `backend-dotnet/Services/AuditService.cs`, `backend-dotnet/Services/FoundationSchemaService.cs` | Auth decisions and operational actions already have audit/correlation storage. | Low | Preserve correlation_id/causation_id on all Stage 9 actions. |
| Mobile backend readiness | `docs/architecture/OPSTRAX_STAGE_9_MOBILE_READINESS_NOTES.md`, `backend-dotnet/Controllers/EndpointMappings.cs` | Mobile-capable backend shaping is expected, but proof/access foundations are missing. | High | Make the backend mobile-ready first; defer UI work. |
| Offline/idempotency mobile behavior | `docs/architecture/OPSTRAX_STAGE_9_MOBILE_READINESS_NOTES.md`, `backend-dotnet/Foundation/FoundationContracts.cs` | Retry-safe creation patterns already exist in foundation services. | Medium | Add idempotency keys and mobile metadata fields to proof/access records. |
| Device/location metadata | `backend-dotnet/Services/TelemetrySchemaService.cs`, mobile notes | Telemetry already captures device/location concepts; proof/access tables should mirror the same mobile-friendly metadata. | Medium | Persist device_id, app version, geo coordinates, and capture timestamps. |
| Notification readiness | `backend-dotnet/Services/NotificationSchemaService.cs`, `backend-dotnet/Controllers/EndpointMappings.cs` | Notifications exist, but Stage 9 should not create a push integration dependency. | Low | Emit events only; let later phases subscribe for mobile notifications. |
| Sales/demo impact | Stage 8 finance docs and mobile notes | Stage 9 materially improves operational credibility for demos and future billing confidence. | Medium | Keep the flow believable, auditable, and not over-claimed. |

