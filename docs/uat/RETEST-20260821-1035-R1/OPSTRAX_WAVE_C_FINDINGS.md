# Wave C — Independent Adversarial Review Findings

Run: `RETEST-20260821-1035-R1`. Reviewers did not author the code they reviewed. Attempt 1 (workflow) **failed** — all 5 agents hit the session usage limit and returned an empty set; that was recorded as FAILED, not as a clean review. Attempt 2 = 4 direct parallel reviewers, each required to attempt self-refutation before reporting.

## UI lens — reported, orchestrator-verified

| # | Sev | Finding | Orchestrator verification | Status |
|---|---|---|---|---|
| C-UI-1 | **HIGH** | The new portal identity boundary (`customer_portal:view` ∧ ¬`dashboard:view`) captures **internal staff roles**, imprisoning them in the chrome-less customer shell | **CONFIRMED**: `EndpointMappings.cs:2335` — `["Customer Service"] = ["customers:view","customer_portal:view","dispatch:view","crm:view"]` — internal role, no `dashboard:view`. Also CRM & Sales Manager (DB seed). Tenant Admin / Fleet Owner / Fleet Manager also hold `customer_portal:view` but are safe because they also hold `dashboard:view`. **Diff-introduced regression.** | MUST FIX |
| C-UI-2 | **HIGH** | Stale navigation: "Audit Logs" nav item still requires `reports.manage`, which Packet 2's alias trim decoupled from `audit:view` — sidebar shows an item whose route now denies | **CONFIRMED**: `moduleConfig.ts:143` `requiredPermission: "reports.manage"` vs `App.tsx:371` route requires `audit:view`. Dispatcher / Fleet Manager / Safety Manager get nav-visible + route-denied → PermissionDenied dead end | MUST FIX |
| C-UI-3 | HIGH-MED | Portal-binding control keyed on role **name** (`/portal/i`) misses the actual portal roles `Customer` and `Customer Viewer` → unbindable dead accounts | **CONFIRMED**: `AdminPage.tsx:327` regex; backend `RequireCustomerBindableRole` (`:14224`) uses `Contains("Portal")`. But `["Customer"] = ["shipments:view","customer_portal:view","alerts:view"]` routes to the portal shell yet cannot be bound. Routing keys on permission shape, binding keys on name — **self-contradictory contract** | MUST FIX (both sides) |
| C-UI-4 | MED | Fail-closed `/login` fallback renders an ordinary login form to an already-authenticated user — no explanation, no sign-out; submitting valid credentials appears to do nothing | Loop is genuinely broken (not a cycle); the defect is the dead end | FIX (dedicated no-workspace screen) |
| C-UI-5 | MED | `ConfirmDialog` focus restore lands on `<body>` (opener unmounts with the dropdown); during `busy` all buttons disable so Tab escapes the trap into the background page | Correctly identifies that `aria-modal` does not stop keyboard Tab | FIX |
| C-UI-6 | MED | Switching away from a portal role keeps the stale hidden `customerId` and submits it → server 400 about an off-screen control | Silent-binding-clear candidate was refuted (list returns `customer_id`) | FIX |
| C-UI-7 | LOW-MED | Merged error slot resurrects a stale server error after the client error clears | | FIX |
| C-UI-8 | LOW | DEF-020 honest-state treatment stops at the KPI row — exports tile/tab and advisor tab still render error as "0 / none" | Same dishonest-zero class the run exists to remove | FIX |
| C-UI-9/10 | LOW | Residual `?? 0` on measured values; zero-customers tenant lacks an honest empty state | | FIX where cheap |

**Overstated-invariant note:** `fleet:view` still reaches `telemetry.devices.read` via the shared `telematics:devices:view` token. Not user-visible harm, but the "symmetric closure severed" reading of the Packet 2 comment is wrong and must not be relied on.

**Cleared as SOUND after refutation:** customer bounce completeness; auth-loading renders neutral (no internal chrome flash); redirect race / bfcache; `/login` self-loop terminates; route shadowing; ConfirmDialog initial focus on Cancel (not the destructive action), Escape-ignored-while-busy, aria wiring, `role="alert"` error slot; error announcement; no silent clearing of existing bindings on edit; dispatcher/fleet_manager nav→guard parity for all other modules; AuditLogs error vs measured-zero distinguishable to screen readers.

**Reviewer environment note:** eslint's flat config **ignores all of `frontend/src/`** — the repeated "lint clean" gate in packet reports gives *zero signal* on these files. Type checking (`tsc --noEmit`) and the contract scripts do provide signal.

## Database lens — reported, orchestrator-verified

| # | Sev | Finding | Orchestrator verification | Status |
|---|---|---|---|---|
| C-DB-1 | **HIGH** | The DEF-023 scope change makes **device revoke fail with 42501 in every protected environment** — the credential kill switch becomes inoperable | **CONFIRMED FROM SOURCE CONTRACT (stronger than a live check).** `2026_08_11_stage76…sql:283-284` **RAISES** `'Stage76 eld_devices credential read boundary is unsafe'` if `opstrax_app` *has* UPDATE on `api_key_hash`/`hmac_secret_encrypted` — so granting them is **contract-forbidden**. DeviceRevoke now runs `RunInTenantTransactionAsync` (≈:18070) on the `opstrax_app` connection and SETs 9 columns it is denied. Suspend/activate survive because stage76 *requires* `status`/`device_state`/`row_version` UPDATE to be granted. **The new test cannot catch it**: `DeviceRevokeLifecyclePostgresTests.cs:113` connects via owner `TestDb.ConnectionString` | MUST FIX |
| C-DB-2 | MED | On a genuinely fresh database, `stage83_platform_settings` runs **before** `opstrax_system` exists (created only by terminal stage58), so its FORCE-RLS/policy hardening is silently skipped — and unrepairably, since it is ledgered and not in the repair list | Guard is `IF EXISTS(rolname='opstrax_system')`; live DBs unaffected (upgrade replicas). Hardening drift, not outage | FIX (one line: add to `repair_migration` list) |
| C-DB-3 | MED-LOW | `DemoTenantSeeder.cs:349-353` seeds users with role names absent from the catalog (`Maintenance Manager`, `Safety Auditor`), recreating the exact `role_id`-NULL rows stage87 exists to eliminate | Verified live: 4 companies each hold exactly these two NULL-role_id users, created *after* stage87 applied | FIX |
| C-DB-4 | LOW | Stage86 closed the column split-brain but not Batch7's **index** half (`idx_audit_logs_module`/`_severity` still runtime-only); SAFE- backfill inherits Batch4's `LPAD(…,5,…)` truncation (ids ≥ 100000 share display keys; no unique index, max id 73) | Parity-intentional, flagged for awareness | OPTIONAL |

**Cleared as SOUND (database lens), with method:** SqlStatementSplitter — executable differential old-vs-new over `001_schema.sql` gives **identical 166-statement output**; all mandated adversarial constructs correct ($1 params, `$a$`/`$ab$` prefix-collision tags, nested block comments, `E'\''`, CRLF, lone trailing `$`, missing trailing `;`, quoted-identifier `;`). Two *constructible* divergences (`$` inside an identifier; non-ASCII dollar tags) exist but no repo SQL contains either, and the splitter's only input is `001_schema.sql`. Stage86 idempotent; **FORCE lift/restore is mid-failure-safe** (single BEGIN/COMMIT, ALTER TABLE transactional, ACCESS EXCLUSIVE until commit — concurrent sessions can never observe FORCE off). Stage87 reproduces `ResolveRoleRecord` exactly; same-scope duplicate role names structurally impossible (unique indexes); cross-tenant assignment impossible by predicate. Runner: stage86/87 **do** apply on a cutover database — verified empirically on `staging_upgrade`. **Readiness format strings: all 8 verified byte-for-byte against the live catalog using the same functions the check uses — no mismatch, `/health/ready` will not go red from these rows** (this was the flagged Sev-1 vector; it is clear). Revoke lock ordering consistent; DEF-027 `deleted_at` filters restore consistency rather than hide history.

## Security / Test lenses

Pending — reviewers still running.
