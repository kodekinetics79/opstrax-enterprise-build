# Production QA Matrix

| Module | Page/Route | Feature/Button | Expected action | Current status | RBAC permission required | Data source | Fix applied | Remaining gap |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Dashboard | `/command-center` | KPI cards, Acknowledge Risks, Generate Brief | Navigate to filtered modules or reports | PARTIAL | `dashboard.view` | API + dev fallback | KPI cards navigate; actions route to alerts/reports | Some KPI values still synthesized from summary service |
| Dashboard | `/live-dashboard` | Export Watchlist, Run AI Brief | Export or refresh live brief | PARTIAL | `dashboard.view` | API + dev fallback | Live shell routes are connected | Some watchlist data is fallback-backed |
| Vehicles | `/vehicles` | View list/detail, Create, Export CSV, Assign Driver | CRUD and assignment actions | PARTIAL | `vehicles:view`, `vehicles:create`, `vehicles:update`, `vehicles:delete`, `vehicles:assign`, `vehicles:export` | API + dev fallback | Centralized service fallback and detail drawer links | Delete/archive and export audit trail still need full backend persistence |
| Drivers | `/drivers` | View list/detail, Create, Export CSV, Assign Vehicle | CRUD and assignment actions | PARTIAL | `drivers:view`, `drivers:create`, `drivers:update`, `drivers:delete`, `drivers:assign`, `drivers:export` | API + dev fallback | Centralized service fallback and detail context | Delete/archive and export audit trail still need full backend persistence |
| Dispatch / Trips | `/dispatch` | Assign, Override, View route, Create trip | Assign/reassign/cancel or open detail | PARTIAL | `dispatch:view`, `dispatch:create`, `dispatch:update`, `dispatch:assign`, `dispatch:cancel` | API + dev fallback | Dispatch board routes and live board are connected | Cancel/status mutation endpoints are still limited in frontend service wiring |
| Shipments / Jobs | `/jobs`, `/shipments` | Create Job, Export Roster, Send ETA, Capture Proof | CRUD, export, and detail actions | PARTIAL | `shipments:view`, `shipments:create`, `shipments:update`, `shipments:delete`, `shipments:export` | API + dev fallback | Jobs page now uses service-backed fallback data | Shipment CRUD/export and archive flows need backend parity |
| Customers | `/customers` | View detail, Create/Edit/Delete | CRUD customer records | PARTIAL | `customers:view`, `customers:create`, `customers:update`, `customers:delete` | API + dev fallback | Service fallback data is centralized | Archive/delete persistence and linked invoice lists need full backend support |
| Alerts | `/alerts` | Acknowledge, View Details, Create Task | Update alert state or open linked detail | PARTIAL | `alerts:view`, `alerts:acknowledge`, `alerts:close` | Dev fallback | Dashboard and alerts surfaces are connected | Alert CRUD is still page-local in some surfaces |
| Maintenance | `/maintenance`, `/work-orders` | Create Schedule, Create Work Order, Defer, Close | CRUD maintenance/work-order records | PARTIAL | `maintenance:view`, `maintenance:create`, `maintenance:update`, `maintenance:close` | API + dev fallback | Service fallback records are centralized | Close/archive paths still need backend confirmation |
| Safety | `/safety`, `/incidents`, `/dashcam`, `/coaching`, `/evidence-packages` | Review, Create Coaching, Create Incident, Export evidence | Open detail, mutate incident/coaching state, export evidence | PARTIAL | `safety:view`, `safety:create`, `safety:update`, `safety:review`, `safety:evidence:view`, `safety:evidence:export` | API + dev fallback | Safety fallback data is centralized | Evidence export permission checks should be tightened per action |
| Compliance | `/compliance`, `/hos-eld` | Acknowledge/Resolve, Export, Update status | View and mutate compliance records | PARTIAL | `compliance:view`, `compliance:update`, `compliance:export` | API + dev fallback | Compliance service fallback implemented | Export and update endpoints still need stronger tenant-scoped verification |
| Reports | `/reports` | Run report, Schedule, Export | Generate or export report data | PARTIAL | `reports:view`, `reports:export` | API + dev fallback | Reports page remains operational | Some report templates are still synthetic but service-backed |
| Admin / Settings | `/settings`, `/audit-logs` | Users, Roles, Permissions, Settings, Audit logs | View/update admin configuration | PARTIAL | `users:view`, `users:create`, `users:update`, `users:delete`, `roles:view`, `roles:update`, `settings:view`, `settings:update`, `audit:view` | API + backend | Permission gate exists on routes | Full user/role CRUD and audit write actions need backend completion |
| Fleet map | `/map-view` | Vehicle/route/event drilldown | Open operational detail and filters | PARTIAL | `map.view` | Dev fallback | Map component now uses centralized fallback data | Live telematics provider integration still pending |
| Login | `/login` | Seeded tenant shortcuts, sign in | Authenticate and open command center | PASS | public login, then session permissions | API + auth fallback | Visible demo wording removed, enterprise-style entry point retained | Demo-auth fallback still enabled for local seeded access |

